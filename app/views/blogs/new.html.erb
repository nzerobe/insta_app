
<% if @blog.errors.any? %>
  <div id="error_explanation">
    <h2><%= pluralize(@blog.errors.count, "error") %> prohibited this <%= @blog.content %> from being saved:</h2>
    <ul>
    <% @blog.errors.full_messages.each do |message| %>
      <li><%= message %></li>
    <% end %>
    </ul>
  </div>
<% end %>

<%= form_with(model: @blog, local: true, url: confirm_blogs_path) do |f|  %>
  <p><%= f.text_area :content %></p>
  <%= image_tag(@bog.image.url) if @blog.image && @blog.image.url %>
  <%= f.file_field :image %>
  <%= f.hidden_field :image_cache %>
  <p><%= f.submit "確認画面へ" %></p>
<% end %>
<%= link_to "一覧に戻る" ,blogs_path %>
