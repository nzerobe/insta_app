
<% if @blog.errors.any? %>
  <div id="error_explanation">
    <h2><%= pluralize(@blog.errors.count, "error") %> エラーによって <%= @blog.content %> を保存できませんでした。</h2>
    <ul>
    <% @blog.errors.full_messages.each do |message| %>
      <li><%= message %></li>
    <% end %>
    </ul>
  </div>
<% end %>

<%= form_with(model: @blog, local: true, url: blogs_path, method: :post) do |f|  %>
  <p><%= f.text_area :content %></p>
  <%= image_tag(@blog.image.url) if @blog.image && @blog.image.url %>
  <%= f.hidden_field :image %>
  <%= f.hidden_field :image_cache %>
  <p><%= f.submit "投稿" %></p>
<% end %>
<%= link_to "一覧に戻る" ,blogs_path %>
