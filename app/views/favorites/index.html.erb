<% @favorites.each do |favorite| %>
  <p><%= link_to image_tag(favorite.blog.image.url), blog_path(favorite.blog.id) %></p>
  <p><%= link_to favorite.blog.content, blog_path(favorite.blog.id) %></p>
<% end %>
